commands:
  create_swap:
    command: /bin/dd if=/dev/zero of=/var/swap.1 bs=1M count=1024 && /sbin/mkswap /var/swap.1 && /sbin/swapon /var/swap.1 && echo "/var/swap.1  swap       swap    defaults        0   0" >>/etc/fstab
    test: test ! -f /var/swap.1
